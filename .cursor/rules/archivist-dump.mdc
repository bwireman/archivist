---
description: Dump indexed decisions into docs/dump for later LLM sessions
alwaysApply: true
---

# Dump docs

Archivist does not generate documentation. After recording a decision, or when wrapping up work that changed architecture, dump indexed context into `docs/dump/`.

Topic dumps stay on **this checkout's index**. Do not add `--adr-scope global` to a query dump unless the topic is the Archivist product.

```bash
make refresh-docs # index, then export repo ADRs and (separately) product ADRs
./archivist dump --type adr --adr-scope repo -o docs/dump/decisions.md
./archivist dump --type adr --adr-scope global -o docs/dump/global-decisions.md
./archivist dump "sqlite vs postgres" -o docs/dump/sqlite.md
```

- `make dump-docs` writes `docs/dump/decisions.md` from the repo DB and `docs/dump/global-decisions.md` from `~/.archivist/global.db`. No Ollama. Keep those two files as separate scopes; do not merge them.
- Index first (`make index`) so new ADRs and code show up; Ollama is required for that. Indexing writes in-repo global ADRs into the machine-wide DB, not `.archivist/index.db`.
- Query dumps are for one topic; use a short filename under `docs/dump/`
- Do not hand-edit `docs/dump/` — regenerate it
- Do not write dumps into `docs/decisions/` or `docs/global-decisions/` (those directories are source ADRs only)
- `docs/dump/` is not indexed, so dumps will not feed back into the index
