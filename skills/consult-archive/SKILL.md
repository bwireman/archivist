# Consult the archive

Before guessing APIs or past decisions:

1. Use MCP `search` or `archivist search "topic"`.
2. Read `docs/archive/INDEX.md` and `docs/archive/rules.md` when MCP is unavailable.
3. Use MCP `check` before implementing risky changes.
